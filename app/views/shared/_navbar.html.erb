<nav class="navbar navbar-default">
  <div class="container-fluid">
 
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><%= link_to "My Saved Pokemons", my_pokemons_path%></li>
        <li><%= link_to "Check All Pokemons", pokemons_path%></li>
      </ul>
      <form class="navbar-form navbar-left">
        <div class="form-group">
          <input type="text" class="form-control" placeholder="Search for a Pokemon Name">
        </div>
        <button type="submit" class="btn btn-default">Search</button>
      </form>
      <ul class="nav navbar-nav navbar-right">

        <% if trainer_signed_in? %>
         <li><%= link_to current_trainer.email, edit_trainer_registration_path%></li>
        <li><%= link_to "Log Out", destroy_trainer_session_path, method: :delete %></li>
        <% else %>
        <li><%= link_to "Sign Up", new_trainer_registration_path%></li>
        <li><%= link_to "Log In", new_trainer_session_path%></li>
        <%end%>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>